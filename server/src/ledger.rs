//! Event and receipt persistence boundary.
//!
//! The current prototype records minimal node telemetry through `gateway`; the
//! ledger module is reserved for structured verifier and execution receipts.
