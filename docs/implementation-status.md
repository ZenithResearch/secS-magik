# secS-magik implementation status

This document is the status ledger for this repository. It separates what is implemented today from what is partially implemented, planned next, or only future/vision. Keep this file current whenever code or docs change.

## Status labels

| Label | Meaning |
|---|---|
| Solid / implemented | Present in current code and covered by tests or direct inspection. Safe to describe as implemented. |
| Partial / prototype | Present in current code, but incomplete, misleadingly named, local-only, or not strong enough for production/security claims. |
| Planned / next implementation | Accepted design direction for the next coding pass. Not yet present in current code. |
| Future / optional rail | Directional or later-stage work. Do not block the first implementation pass on it unless explicitly promoted. |
| Out of scope | This repository should not own it. Reference only as a boundary. |

## Current solid / implemented surface

| Surface | Location | Status | What is solid |
|---|---|---|---|
| Rust workspace | `Cargo.toml` | Solid / implemented | Workspace members are `core`, `client`, and `server`. Workspace tests passed after Issue 4.2; A0/A1 are docs-only checklist/status reconciliation slices and do not change runtime code. |
| `ZenithPacket` v0 shape | `core/src/lib.rs` | Solid / implemented | The packet has `session_id`, `nonce`, `opcode: u8`, `proof`, `claim_ttl`, `encrypted_payload`, and `mac`; serialization round-trip tests pass. |
| Standard opcode constants | `core/src/lib.rs` | Solid / implemented | `OPCODE_GENERATE = 0x01` and `OPCODE_CHAT = 0x02` exist and are tested. |
| CLI decimal opcode parsing | `client/src/main.rs` | Solid / implemented | `hub 16 ...` parses as decimal `u8`; `0x10` and values above `255` are rejected by current tests. |
| Client packet construction | `client/src/main.rs` | Solid / implemented | CLI builds and sends `ZenithPacket` over TCP using current proof helper. |
| Tunnel helper functions | `core/src/tunnel.rs` | Solid / implemented | ChaCha20Poly1305 helper tests cover round trips and tamper/wrong-key rejection. |
| Ed25519 helper primitives | `core/src/zk.rs` | Solid / implemented as primitive | Signature/proof helper tests pass. These primitives are not yet a full server verifier. |
| Session store | `server/src/session.rs` | Solid / implemented as local utility | In-memory session-store tests pass. This is not yet replay/session binding for the verifier pipeline. |
| Retired legacy direct TCP entrypoint | `server/src/lib.rs`, removed `server/src/main.rs` | Solid / implemented as entrypoint hardening | The old `run_node` / `PayloadRouter` direct opcode dispatch path and implicit `server` binary are gone, so the packaged server no longer exposes that bypass around verifier/replay/receipt boundaries. |
| Canonical prototype gateway binary | `server/src/bin/secs-gateway.rs` | Partial / prototype | Thin wrapper over library modules for prototype ingress, proof/TTL check, explicit runtime-mode payload handling, manifest descriptor lookup, signed context creation, SQLite receipt/event ledger, legacy telemetry, and opcode routing. |
| Historical secZ compatibility binary | `server/src/bin/secz.rs` | Partial / prototype | Thin compatibility wrapper for the old command name; not canonical verifier ownership. |
| Prototype ingress | `server/src/ingress.rs` | Partial / prototype with targeted audit hardening | Deserializes packets only after bounded wire reads and explicit logical `proof` / `encrypted_payload` length prechecks, decrypts payloads by the validated runtime config instead of ambient env, rejects production use of dev/prototype or legacy prototype-evidence descriptors before signed-context issuance, emits reject receipts for typed verifier/payload/manifest failures, and hands verified payloads to the gateway router. Issue #77 also makes descriptor-only `production_verified` runtime verification fail closed for the canonical `0x44` `membership.provision` descriptor rather than signing a context from descriptor metadata alone. The read/memory and accepted-task boundaries are hardened, but live runtime ingress now accepts a bounded versioned request envelope with evidence refs/public inputs for the configured evidence adapter path; this is still local verifier/runtime plumbing, not production deployment proof or live registry discovery, and #144/#160 still gate closing #73. |
| Prototype gateway/router / bounded execution broker | `server/src/gateway.rs` | Solid / implemented as receiver-local bounded handler routing | Preserves legacy `node_telemetry`, persists verify/execution receipts and handler lifecycle events in the ledger, carries an explicit expected receiver audience from runtime config, revalidates signed contexts against the active manifest before execution, selects handlers by descriptor `handler_id`, enforces payload/output size and timeout limits, streams subprocess output under a hard byte cap, kills timed-out subprocess process groups where supported, emits execution receipts for success/failure/unavailable/timeout/oversized outcomes, and gates dev subprocess handlers out of `production_verified` runtime bindings. This is not a durable distributed broker or broad shell authority. |
| Production-shaped gateway runtime config/readiness | `server/src/config.rs`, `server/src/bin/secs-gateway.rs`, `server/tests/{runtime_config,readiness}.rs`, `scripts/production-gateway-smoke.sh` | Solid / implemented as local runtime hardening | Canonical gateway startup reads typed runtime config; `production_verified` requires explicit non-fixture receiver audience, verifier key path, trust-registry path, bind address, DB/ledger path, ingress/handler/output limits, and max in-flight connection cap before serving; startup rejects missing/malformed/empty/fixture-only trust registries and `local_static` adapters unless the operator explicitly enables fixture-only smoke; readiness can distinguish config-loaded, ledger-ready, and trust-registry-ready/fixture-only state; the smoke script starts the real gateway binary and sends malformed/oversized TCP input under fixture-only env without real secrets. This is not deployed production and does not complete wallet-core parity, Track I membership.provision E2E, or live federation discovery. |
| Receipt/event ledger | `server/src/{ledger,receipt,schema}.rs`, `server/tests/{ledger,receipt,gateway_layout}.rs` | Solid / implemented as local ledger | Creates `events` and versioned `receipts` tables with runtime SQL, stores signed receipt metadata including `authenticator_kind`, `signer_key_id`, signature length/digest for operator inspection, and `context_id` where a verified context exists; exposes redacted operator inspection by receipt id or context id while keeping raw payload/private evidence and raw signature bytes out of the export surface. |
| Node/verifier identity and public-key seam | `server/src/identity.rs`, `server/tests/identity.rs` | Solid / implemented for explicit local file config plus B4 own-verifier lifecycle seam | `production_verified` requires an explicit regular owner-private verifier key file before signing; missing, inaccessible, malformed, world-readable, and symlink key files fail as typed identity config errors; default signer ids are deterministic Ed25519 public-key fingerprints; unsafe override ids that look like paths or secret material reject; the public identity API exposes signer id/public key/signing helpers without exposing raw secret bytes; contexts/receipts can be verified through a receiver-held `PublicVerifierKeyRegistry` with duplicate-key-id fail-closed behavior and explicit receipt-signing-time validity checks; registry checks fail unknown ids, wrong-key signatures, revoked keys including effective `revoked_at` metadata, expired keys, unknown-status keys, and not-yet-valid keys; key entries carry `status`, `not_before`, `not_after`, `revoked_at`, `replaced_by`, and a fail-closed `production_authority` flag; replacement metadata is not automatic trust transfer; production-authority verification accepts configured non-local `ed25519_node_and_verifier` identities with `ed25519` key metadata and rejects local/dev/test fixture keys or other authenticator/algorithm metadata. |
| Trusted issuer/root production evidence policy | `server/src/evidence.rs`, `server/tests/{production_federated,trust}.rs`, `server/tests/support/{trust_fixtures,wallet_fixtures}.rs` | Solid / implemented on main after PR #69 | Track E adds typed `membership_credential` and `provisioning_credential` evidence kinds, receiver-held static `TrustedIssuerEntry` registry metadata, registry JSON loading, signed fixture credential verification, issuer/key/credential status checks, `trust_root_ref` / `registry_root_ref` matching, descriptor-local production evidence policy, wallet+issuer proof-layer composition, A6 policy matrix coverage, safe evidence summaries, typed reason-code coverage, and redacted operator-inspection distinctions. `local_static`, plaintext/prototype evidence, wallet-only proof, embedded caller keys, and caller-supplied root refs cannot satisfy production descriptors by themselves. This is static fixture authority merged to main in PR #69 with post-merge CI passing; it is not live Castalia/Dregg discovery, Midnight/Cardano authority, production deployment proof, public auditability, or full Castalia Wallet wallet-core parity. |
| Track I local `membership.provision` E2E | `server/src/manifest.rs`, `server/tests/production_federated.rs`, `server/tests/support/trust_fixtures.rs` | Solid / implemented on main after PR #76 plus #84 negative proof | PR #76 added canonical `0x44` `membership.provision` descriptor, no-real-secret wallet+membership credential fixture composition, signed context creation, bounded handler execution, local/operator verify+execute receipt inspection, focused reject matrix, replay/session/TTL guards, and summary/operator-inspection redaction tests. #84 adds the explicit proof that evidence-backed verifier acceptance, fixture smoke/log output, or handler-unavailable routing is not `membership.provision` success unless the same context has an accepted execute receipt. PR #76 merged to `main` at `5e5bb71` and post-merge Rust CI run 27071532041 passed. This is local production-shaped E2E only; it is not production deployment proof, public auditability, live registry discovery, Midnight/Cardano authority, or full wallet-core parity. Post-merge review follow-ups #78-#83 remain open. |
| `membership.provision` runtime evidence guard | `server/src/verifier.rs`, `server/tests/production_federated.rs` | Solid / implemented for #77 descriptor-only runtime boundary | Issue #77 adds a production-only fail-closed guard for descriptor-only `production_verified` runtime verification of the canonical `0x44` `membership.provision` descriptor when no wallet proof-of-possession and trusted issuer evidence adapter path is involved. Evidence-backed helper verification still succeeds for Track I local E2E, and local/dev descriptor-only behavior is intentionally unchanged. This is a boundary guard, not live ingress wallet+issuer evidence verification or active runtime authority. |
| Receiver-local manifest descriptors | `server/src/manifest.rs` | Solid / implemented as descriptor layer | Defines `OperationDescriptor`, `ReceiverManifest`, opcode range classification, seeded v0 descriptors for `0x01`, `0x02`, `0x10`, `0x20`, `0x30`, and `0x44` `membership.provision`; M15.5 (#141) composes canonical `membership.provision` from `wallet_presentation` + `membership_credential` + `dregg_authority` evidence. Typed unknown-opcode lookup errors remain fail-closed. |
| Payload handling | `server/src/payload.rs`, `core/src/tunnel.rs` | Solid / implemented | Parses tunnel keys and enforces explicit runtime-mode payload behavior. Tunnel AEAD is bound to the packet envelope via canonical associated data (`session_id` (16) \|\| `opcode` (1) \|\| `claim_ttl` (8, BE)); a captured ciphertext re-bound to a different envelope fails authentication as `bad_mac` before routing and creates no replay reservation. This is AEAD context-binding only — not per-session key agreement, forward secrecy, or transport TLS. |
| membership.provision runtime binding (#78) | `server/src/membership.rs`, `server/src/gateway.rs` | Solid / implemented as handler registration only, not authority | Default runtime bindings register the bounded native `membership/provision` handler in every mode, ending the advertised-but-unbound descriptor mismatch; handler binding is not authority. Reaching it requires an evidence-backed verifier-signed context: #77 keeps descriptor-only production verification fail-closed, live ingress carries evidence refs/public inputs through the #162 versioned request envelope when configured, and dev subprocess handlers remain unregistered in production. Handler defines no membership business semantics — the v0 effect is the verify+execute receipt chain. |
| Multi-evidence-ref contract (#79) | `server/src/evidence.rs`, `server/src/verifier.rs` | Solid / implemented as verifier/caller API only | `EvidenceInputs` + the canonical multi-ref helper make evidence refs and public inputs explicit caller inputs with deduplication, fail-closed empty refs, typed rejects, and redaction-safe summaries; Track I E2E no longer relies on test-only adapter mutation. Live TCP ingress is now wired with a versioned request envelope for evidence refs/public inputs: `server/src/ingress.rs` decodes bounded refs/inputs and routes them to the canonical evidence adapter path. The #77 descriptor-only guard still applies when refs/adapter input are absent; #162 does not claim production deployment, live registry discovery, or live Dregg proof/finality. |
| Active-manifest authorization binding (#81) | `server/src/manifest.rs`, `server/src/gateway.rs`, `server/src/verifier.rs` | Solid / implemented on the Phase 1 branch | `OperationDescriptor::authorization_fingerprint()` is a deterministic SHA-256 over every routing/authorization-relevant descriptor field (canonical length-prefixed bytes; no payload/evidence material). The verifier stamps it onto `VerifiedCallContext.descriptor_fingerprint` (schema v2) at verification time, and `signed_context_matches_active_manifest()` re-checks the fingerprint and the actual TTL span against the active manifest before any route side effect. A trusted verifier can no longer route a signed context whose operation/handler id match while other descriptor-derived authorization fields are stale; `server/tests/gateway_layout.rs` proves a mutated-variant manifest yields exactly one inspectable `descriptor_context_mismatch` reject with zero side effects. Descriptor-binding hardening only — not evidence-aware live ingress (#162/#144) or Dregg authority (#73). |
| WASM permission control panel (M13.4b, #127) | `permissions/` (`secs-permissions` crate), `panel/` (wasm + `www/`), `.github/workflows/rust.yml` | Solid / implemented as a vanilla browser panel | The permission model is now the standalone wasm-compatible `secs-permissions` crate (`server::permissions` re-exports it; gateway/CLI/panel/M14/M15 all share it). The `panel` `wasm-bindgen` crate exposes `grant`/`revoke`/`evaluate`/`list` as JSON-in/JSON-out; `panel/www` is a no-framework browser UI holding the policy in `localStorage` — no server, no network. `cargo check -p panel --target wasm32-unknown-unknown` is in CI; the bundle builds with `wasm-pack`. Receiver-local only — no Dregg authority (#73), deployment proof (#33), or public auditability (#37). |
| Operator permission CLI (M13.4a, #125) | `server/src/permctl.rs`, `server/src/bin/secs-permctl.rs`, `server/tests/permctl.rs` | Solid / implemented as the CLI half of the control panel | `secs-permctl` authors/inspects receiver-local permission policies: `list` / `grant` / `revoke` / `evaluate` over a JSON policy file, fail-closed on malformed input. Logic is in the unit-tested `server::permctl` library; the WASM browser panel (M13.4b) wraps the same model. Receiver-local only — authors `authority_source = receiver_local`; no Dregg authority (#73), deployment proof (#33), or public auditability (#37). |
| Live ingress permission enforcement (M13.3, #123) | `server/src/gateway.rs`, `server/src/verifier.rs`, `server/tests/permissioned_file_write_e2e.rs` | Solid / implemented end-to-end on the demo path | `VerifiedCallContext` carries a signed `resource`; `ConfigurableRouter` has an installable manifest + optional `PermissionPolicy`, and `route_verified` evaluates the policy fail-closed (caller × opcode × operation × resource) after the verify receipt and before handler dispatch, recording a typed `permission_*` execute-reject on denial. The sandboxed handler writes the context-bound resource. E2E proves allow + deny matrix (wrong-resource/revoked/expired/empty-policy/replay) with no unintended write and redacted receipts. **Receiver-local only** — not Dregg authority (#73), deployment proof (#33), or public auditability (#37). Control panel (M13.4 = TUI + WASM) and demo script (M13.5) follow. |
| Sandboxed demo.file.write handler (M13.2, #121) | `server/src/file_write.rs`, `server/tests/file_write_handler.rs`, `docs/issues/secs-magik-phases/demo-file-write-handler.md` | Solid / implemented as an isolated demo handler (not yet router-wired) | `DemoFileWriteProgram` writes a JSON payload's content to a `file://` target only inside a configured sandbox root; traversal/outside-sandbox/symlink-escape/non-`file://` rejected before any write with typed `demo_file_write_*` reasons. `demo_file_write_descriptor(0x50)` is dev-bounded and absent from `default_v0` (installed explicitly), so production runtime rejects it. **Demo handler only** — not arbitrary filesystem authority, deployment proof (#33), public auditability (#37), or Dregg authority (#73); production-hardening backlog documented. Router/config wiring + live E2E matrix are M13.3. |
| Receiver-local permission policy (M13.1, #119) | `server/src/permissions.rs`, `server/tests/permissions.rs` | Solid / implemented as a standalone policy module (not yet gateway-wired) | `PermissionRecord` binds caller × opcode × operation × resource scope (exact/prefix) × validity window × status, with a typed `authority_source` (`receiver_local` now; `dregg_backed`/`dregg_authority` reserved for M14/M15). `PermissionPolicy::evaluate` is fail-closed (default-deny), deny-wins, and reports typed `DenyReason`s; `from_json_file` loads a receiver-local policy and fails closed on empty/malformed/invalid records. **Receiver-local policy only** — not Dregg capability/revocation authority (#73), live registry discovery (#72), deployment proof (#33), or public auditability (#37); non-`receiver_local` sources are not evaluable here and fail closed. Gateway/ingress wiring, the sandboxed `demo.file.write` handler, and the E2E matrix are M13.2/M13.3. |
| Credential-summary disclosure boundary (#83) | `server/src/evidence.rs`, `server/tests/production_federated.rs`, `docs/issues/secs-magik-phases/credential-summary-disclosure-boundary.md` | Solid / implemented on the Phase 1 branch | Field-by-field disclosure taxonomy for `membership_credential`/`provisioning_credential` summaries: linkable opaque handles `credential_id`/`status_ref` digested by default (joining `evidence_ref`/`issuer_key_id`), receiver-held authority anchors (`issuer_id`, trust/registry roots) + `status` outcome + validity window + kind/suite cleartext, raw material absent. Digests are deterministic (operators correlate without raw values). Local/operator inspection boundary only — not public auditability (#37) or deployment proof (#33); #70 wallet+issuer composition and #77 runtime guard unchanged. |
| Production target kind (#82) | `server/src/manifest.rs`, `server/tests/support/trust_fixtures.rs` | Solid / implemented on the Phase 1 branch | `TargetKind::ReceiverProductionHandler` gives production-shaped receiver handlers an explicit non-dev, non-legacy label. Canonical `0x44` `membership.provision` and the production-shaped `trusted_descriptor()` fixture use it (still `dev_binding: false`); dev/legacy descriptors keep `LocalDevProcess`/`LegacyCoreExample`. Bound into the #81 fingerprint, so the `0x44` fingerprint changes. Semantic/auditability cleanup only — target kind is not authority; #77's fail-closed descriptor-only runtime guard for `membership.provision` is unchanged. |
| M12 demoable milestone | `examples/m12-demo.sh` | Solid / implemented as local demo surface | Scripted end-to-end demo against the live local gateway: authenticated caller accept with the returned signed decision, forged-proof/unknown-caller/replay/expiry typed rejects, wallet + Dregg-shaped evidence composition at the adapter seam, and operator receipt-chain inspection — each step asserted, failing the script on any miss. Local verifier behavior only; no production deployment, public auditability, wallet-core parity, live discovery, or Dregg/Midnight/Cardano authority claims; live evidence-aware ingress lands in #162 while #144 finalizer/#160 resource-lock scope remain open. |
| Dregg-shaped evidence seam | `server/src/evidence.rs`, `server/src/manifest.rs` | Solid / implemented for M12.3 (shape+signature only) | `DreggReceiptFixture` (`secs-dregg-receipt-shape-v1`) + `DreggShapedEvidenceAdapter` verify Dregg-shaped receipt/capability-reference presentations: required shape, receiver-held author-key fingerprint match, Ed25519-only suite, time window, request bindings, and the author signature over canonical bytes — failing closed with existing typed reasons; summaries are redaction-safe and never claim public/consensus proof; composition keeps every descriptor-required kind enforced; the demo descriptor is dev-bounded and outside `default_v0`. Not Dregg finality/capability/nullifier/CapTP/revocation authority — #73 stays open. |
| Caller decision response | `core/src/response.rs`, `server/src/ingress.rs`, `server/src/gateway.rs`, `client/src/main.rs` | Solid / implemented for M12.2 | Every gateway connection answers with at most one versioned, redaction-safe `DecisionResponse` frame (decision, typed reason, context/receipt references) written under a bounded timeout; `route_verified` returns the decision projection; the client prints the decision and exits non-zero on reject. Local decision projection only — not handler output, not public auditability (#37), not deployment proof (#33). |
| Caller proof-of-origin authentication | `server/src/caller.rs`, `core/src/caller_proof.rs`, `server/src/verifier.rs`, `server/src/config.rs`, `client/src/main.rs` | Solid / implemented for M12.1 | Receiver-held `CallerKeyRegistry` (status, validity window, revocation, duplicate-id fail-closed) verifies the caller's Ed25519 signature over canonical envelope bytes (session, nonce, opcode, TTL, payload) before signed-context creation; the context subject/key id reflects the authenticated caller instead of the prototype subject. `production_verified` requires `SECS_CALLER_REGISTRY_PATH` at config parse and startup readiness, with the same fixture-only smoke allowance as the trust registry; caller-auth rejects emit inspectable reject receipts and never create replay reservations. The client persists a stable owner-private caller key (`SECS_CALLER_KEY_PATH`) and signs canonical bytes. Caller proof-of-origin is necessary, never sufficient — it does not satisfy wallet/issuer/Dregg evidence and does not unlock `membership.provision`. |
| Supply-chain CI gate | `.github/workflows/rust.yml`, `SECURITY.md`, `.github/dependabot.yml` | Solid / implemented as dependency-hygiene gate | M12.7 adds a `cargo audit` CI job that fails on known RUSTSEC vulnerabilities, bumps `sqlx` to 0.8 past RUSTSEC-2024-0363 (dropping unused MySQL/Postgres backends and the unfixed `rsa` advisory with them), reconciles `uniffi` to the workspace 0.28 pin, and adds SECURITY.md plus weekly Dependabot. A clean scan is dependency hygiene only — not production deployment proof (#33) and not a security audit. |
| Ready-for-prod checklist A0–A9 | `docs/plans/2026-06-02-ready-for-prod-checklist.md` | Solid / implemented as docs/control surface | Track A is complete through A9 as a docs/control-surface phase: production definition, status reconciliation, rail taxonomy/non-goals, identity/key lifecycle gate, wallet-core gate, federated evidence model, production policy matrix, first E2E shape, phase/branch/PR issue checklist, and future-rail deferral. This does not implement Tracks B–I runtime behavior. |

## Current partial / prototype behavior to name carefully

| Behavior | Current fact | How to describe it |
|---|---|---|
| Proof verification | The prototype envelope check (non-empty `proof`, positive `claim_ttl`) still runs first; with a configured caller registry the `proof` field must additionally carry a versioned caller proof-of-origin envelope whose Ed25519 signature over canonical envelope bytes verifies against the receiver-held key. | “Prototype envelope check plus caller proof-of-origin” — caller authentication proves who sent the packet, never membership/issuer/root authority, and is still not ZK verification. |
| Payload security | Tunnel decrypt works if key is configured; plaintext is only allowed when `SECZ_RUNTIME_MODE=local_dev_plaintext` or `SECS_RUNTIME_MODE=local_dev_plaintext`. | “Explicit runtime-mode payload handling,” not silent production plaintext fallback. |
| secZ compatibility file | `server/src/bin/secz.rs` exists as a thin compatibility wrapper. | “Historical command compatibility wrapper,” not the canonical verifier or client architecture. |
| secS verifier | secS parses/inspects and routes; typed evidence adapter calls can now feed signed contexts, but the full staged verifier does not exist. | “Target verifier substrate with local evidence seam,” not fully implemented verifier. |
| Manifest-to-execution wiring | The gateway creates a signed context from descriptor lookup, refuses production-signed dev/prototype descriptors and legacy descriptors backed only by prototype proof evidence, revalidates signed context opcode/operation/handler consistency against the active manifest, and handler selection uses the descriptor `handler_id`, with production runtime bindings withholding dev subprocess handlers by default. | “Descriptor-bound local execution broker,” not a durable distributed broker, arbitrary shell surface, or production trusted-issuer/wallet evidence verifier. |
| Telemetry/audit | `node_telemetry` still stores opcode and payload size for compatibility; `events` and versioned `receipts` now persist typed local audit records with signed receipt metadata and redacted operator inspection/export helpers. Retention is local SQLite database retention until operator rotation/deletion; no automatic remote retention or anchoring exists. | “Local SQLite receipt/event ledger plus legacy telemetry,” not public audit proof. |
| Trusted issuer/root lifecycle | Track E implements the static receiver-held trusted issuer/root fixture policy on main after PR #69: `TrustedIssuerEntry` metadata controls trusted issuer/root acceptance, signed `membership_credential` / `provisioning_credential` fixtures verify against receiver-held issuer keys, status/root/descriptor-local policy checks are tested, and local/dev/wallet-only evidence cannot satisfy production descriptors by itself. | “Static fixture trusted issuer/root policy on main after PR #69,” not live Castalia/Dregg discovery, cryptographic revocation proof, public auditability, deployment proof, full wallet-core parity, or Track I membership.provision E2E. |
| Dregg/Midnight/Cardano | No external runtime dependency in current workspace. M15.1 `docs/specs/dregg-authority-rail.md` is the docs-only `dregg_authority` semantics gate for #137 / #73. M15.2 (#138) adds the receiver-held static `DreggAuthorityRegistry`, fixture, startup/readiness requirement via `SECS_DREGG_AUTHORITY_REGISTRY_PATH`, and typed root/epoch/status binding rejects when `dregg_authority` is enabled. M15.3 (#139) adds the `DreggAuthorityEvidenceAdapter` policy-admission seam: `dga1_`-shaped tokens are accepted only after receiver-held issuer/root/epoch policy lookup and subject/tool/time checks; M12.3 `dregg_receipt` shape-only fixtures cannot satisfy `dregg_authority`. M15.4 (#140) enforces revocation/freshness/finality policy bits fail-closed: missing revocation checks, future/stale status, expired-at-validation tokens, missing finality, not-final, and equivocated material all map to typed rejects. M15.5 (#141) composes `dregg_authority` into canonical `membership.provision` so wallet + issuer + Dregg authority are all required; M12.3 `dregg_receipt` remains shape-only and cannot satisfy production authority. M15.6 (#142) adds local operator inspection/disclosure for Dregg authority with `authority_class:dregg_authority`, `tier:m15_production_shaped`, digested opaque refs (`evidence_ref_sha256`, `issuer_key_id_sha256`, `root_ref_sha256`, `epoch_id_sha256`, `federation_id_sha256`), stable status/finality names, and redacted verify receipt/ledger inspection summaries. M15 proof hardening / #159 proof/finality blocker posture adds receiver-held `expected_revocation_root` binding with `missing_revocation_root` / `wrong_revocation_root`, and explicit blocker modes for federation `RevocationVerifier` / `RevocationTree`, `ReceiptQc::Threshold` with BLS FederationCommittee, and `rotated_replay::verify_rotated_replay_chain`, returning `unsupported_revocation_verifier`, `unsupported_bls_threshold_finality`, and `unsupported_rotated_replay_verifier` rather than accepting fixture status as live proof. | “Static receiver-held Dregg issuer/root/epoch registry plus bounded fail-closed policy-admission posture,” no live Dregg revocation proof, no BLS threshold finality, no rotated-replay proof verification, discovery, Midnight, Cardano, deployment proof, or public auditability. |

## Planned / next implementation surface

These are accepted next-pass targets after the completed issue train and the A0/A1 ready-for-prod reconciliation. They are not yet implemented unless a later code change lands them.

| Target | Planned location | Status |
|---|---|---|
| Track I `membership.provision` E2E | GitHub #70; PR #76; #77 runtime guard | Complete for local production-shaped E2E on `main` at `5e5bb71` with post-merge CI run 27071532041; proves packet construction, wallet + trusted issuer evidence composition, signed context, verify/execute receipts, inspectable local/operator ledger rows, reject matrix, replay/session/TTL guards, and redaction checks. Issue #77 adds the fail-closed descriptor-only production runtime guard. Remaining live-ingress/runtime evidence verification is tracked separately in #162/#144 live TCP evidence-ref/public-input follow-ups and must not be claimed as implemented. |
| Production deployment proof | GitHub #33 | Planned after local E2E; real deployment proof remains separate from local smoke. |
| Public auditability beyond local SQLite receipts | GitHub #37 | Future/public audit rail; local operator inspection is not public auditability. |
| Castalia wallet-core parity | GitHub #71 | Future reconciliation before full wallet-core parity claims. |
| Live Castalia registry discovery | GitHub #72 | Future live registry rail beyond static fixture registry. |
| Dregg / Midnight / Cardano authority rails | GitHub #73/#74/#75 | Dregg M15.2–M15.6 has a bounded static receiver-held policy-admission/operator-inspection seam; #159 adds explicit expected-revocation-root binding and hard-blocker modes for live proof/finality surfaces. Full resource-lock/live-ingress/finalizer hardening remains #160/#162/#144. Midnight/Cardano remain future optional rails. |
| Repository schema / module layout | `docs/repository-schema.md`, `server/src/{ingress,gateway,payload,manifest,evidence,receipt,ledger}.rs` | Phase 0.1 implemented: reusable gateway/payload/ingress code moved out of binaries, and placeholder module homes exist for manifest/evidence/receipt/ledger. |
| OperationDescriptor / ReceiverManifest | `server/src/manifest.rs` | Implemented as a receiver-local descriptor/lookup layer; signed-context creation and local bounded handler routing now consume descriptor operation/handler metadata. |
| Opcode range governance | `server/src/manifest.rs`, docs; possibly `core/src/lib.rs` constants | Implemented in the manifest descriptor layer for reserved/core/candidate/operator ranges. |
| VerificationError / verifier pipeline | `server/src/verifier.rs` | Partially implemented: typed errors and prototype envelope/signature context helpers exist; full staged verifier pipeline still planned. |
| SignedVerifiedCallContext | `server/src/verifier.rs` | Implemented for Ed25519 context signing/verification; verify receipts can now be constructed from signed contexts. |
| Identity/signature helpers for contexts/receipts | `server/src/identity.rs` | Implemented through B4: production identity loading requires an operator-provided key path, default `signer_key_id` is derived as `ed25519:<sha256-public-key-fingerprint>`, safe explicit overrides reject path/secret-shaped ids, contexts/receipts sign with the configured identity, the gateway verifies signed contexts against its local own-verifier registry before emitting verify/execute receipts or running handlers, local/dev receipts remain stamped `local_dev_untrusted`, and `PublicVerifierKeyRegistry` provides a local own-verifier key-status seam for unknown-id, wrong-key, revoked, expired, unknown-status, not-yet-valid, and local-dev-as-production-authority rejection. Trusted issuer/root registry policy remains Track E. |
| Explicit runtime modes | `server/src/runtime_mode.rs` | Implemented for current gateway; local plaintext requires explicit `local_dev_plaintext`, default is `production_verified`. |
| Receipt types | `server/src/receipt.rs` | Implemented for reject, verify, execute, and forward receipts with explicit `RECEIPT_SCHEMA_VERSION = 2`; receipts derived from verified contexts carry `context_id` for local chain inspection and future migration/export compatibility. |
| Event/receipt ledger | `server/src/ledger.rs` | Implemented with runtime SQL and in-memory SQLite tests; gateway/ingress write reject, verify, execution, and handler lifecycle audit records. DDL is centralized in `server/src/schema.rs` rather than embedded inline in ledger/gateway runtime methods. Operator inspection returns redacted local rows by receipt id or context id with export schema version, receipt schema version, reason codes, metadata hashes/hex, signature digest/length, and schema-versioned redacted evidence summaries rather than raw payload, private evidence, or raw signature bytes. |
| Runtime schema ontology | `server/src/schema.rs`, `server/tests/schema.rs` | Implemented as a central runtime SQLite schema ontology for `events`, `receipts`, `replay_reservations`, and legacy `node_telemetry`; preserves the Track C unique replay boundary while keeping telemetry separate from the receipt/replay ledger table set. |
| Prototype receiver ontology | `server/src/ontology.rs` | Implemented as a central home for prototype receiver constants currently reused across ingress/gateway/verifier: default receiver audience, local prototype subject, local test audience/origin, local prototype signer id, and replay/prototype operation reason strings. Future work should continue moving repeated semantic constants here or into a typed configuration surface rather than duplicating string literals. |
| EvidenceAdapter trait | `server/src/evidence.rs`, `server/tests/evidence.rs` | Solid / implemented | Typed adapter boundary with request/result fields for subject, audience, operation, resource, evidence refs, public inputs, and reason codes. |
| `local_static` evidence adapter | `server/src/evidence.rs`, `server/tests/evidence.rs` | Solid / implemented as local-dev-test only | Deterministic local/dev/test scaffold that can satisfy local/dev descriptor evidence requirements and flow into signed contexts/receipts without claiming production authority or adding Dregg/Midnight/Cardano dependencies. Production descriptors reject it as sufficient authority. |
| Track E trusted issuer/root policy | `server/src/evidence.rs`, `server/tests/{production_federated,trust}.rs`, `server/tests/support/trust_fixtures.rs` | Solid / implemented on main after PR #69 | Static receiver-held `TrustedIssuerEntry` registry policy, JSON registry loading, signed `membership_credential` / `provisioning_credential` verification, issuer/key/credential status checks, `trust_root_ref` / `registry_root_ref` matching, descriptor-local evidence gates, wallet+issuer composition tests, policy-matrix coverage, safe summaries, and typed reason-code tests are implemented after E1–E10. This is fixture/static authority merged to main in PR #69 with post-merge CI passing; no live registry discovery, public auditability, deployment proof, Track I E2E, or Dregg/Midnight/Cardano authority is claimed. |
| Track D wallet presentation verification | `server/src/evidence.rs`, `server/tests/{wallet_challenge_contract,wallet_presentation}.rs` | Solid / implemented as temporary minimal-equivalent secS contract | `wallet_presentation` verifies signed presentation/challenge evidence cryptographically over an explicitly temporary secS challenge contract that binds subject, audience, origin, operation, resource, nonce, issued/expires timestamps, signature suite, and a deterministic public-key fingerprint reference. Valid fixtures satisfy without `ShapeValidatedSignatureUnsupported` as proof-of-possession for the claimed subject key; mismatched public-key ref/key material, wrong signature/key/subject/audience/origin/operation/resource/replay/expiry/future-issued/malformed/missing evidence reject with typed reasons. This is not a full Castalia Wallet wallet-core import/parity, not production deployment, and not sufficient issuer/root authority; Track E supplies a separate static trusted-issuer fixture policy on main after PR #69. |
| Track C replay/session/expiry enforcement | `server/src/{ledger,gateway,verifier}.rs`, `server/tests/{gateway_layout,verifier_context}.rs` | Solid / implemented as receiver-local/local durable replay/session/expiry enforcement | Track C is implemented with bounded claims: Duplicate `(session_id, opcode, nonce, replay_scope)` verified contexts reserve atomically in local SQLite, including concurrent identical routes, and duplicates reject with `replay_detected` before handler execution; descriptor max TTL overclaims reject with `claim_ttl_exceeds_descriptor_max` before signed context issuance; all-zero session IDs reject with `invalid_session` before signed context issuance; Expired/wrong-audience/invalid-signature signed contexts emit signed reject receipts/events before replay reservation and before handler execution; Pre-verification/signature failures do not consume replay slots. This is within the configured receiver-local replay store/scope, not distributed/global/cross-Hub/cluster-wide replay protection. |
| Wallet presentation adapter | `server/src/evidence.rs`, `server/tests/wallet_presentation.rs` | Solid / implemented as temporary minimal-equivalent secS contract | Defines typed wallet presentation fixture fields for subject, audience, origin, challenge, signature, public key, replay nonce, and validity window; verifies signatures cryptographically against the D1 contract; binds `public_key_ref` to the supplied public key fingerprint; fails closed for missing/invalid shape and distinguishes wrong signature/key/subject/audience/origin/operation/resource/replay/expiry/future-issued/malformed cases. Full Castalia Wallet wallet-core import/parity remains future reconciliation work. |
| Bounded execution broker accepting verified context | `server/src/gateway.rs`; future `server/src/execution.rs` | Solid / implemented as local bounded broker: handlers consume `VerifiedCallContext`, signed contexts are revalidated against active manifest descriptors, dev/prototype descriptors cannot execute in production runtime, handlers are selected by descriptor `handler_id`, subprocess output is streamed under a hard cap, timed-out subprocess process groups are killed where supported, max in-flight gateway tasks are configurable, execution receipts cover every routed handler outcome, and dev subprocess handlers are not exposed in `production_verified` bindings. Future work may extract this local bounded routing code into a dedicated execution module without widening durability or distribution claims. |
| Packet-builder helper | `core/src/packet_builder.rs` | Solid / implemented as verifier-free construction helper | Builds `ZenithPacket` v0 from caller-provided envelope fields without validating capabilities, credentials, evidence, authority, replay, or verifier receipts. |

## Future / optional rails

| Rail | Status | Do not claim |
|---|---|---|
| Wallet presentation adapter | Implemented as cryptographic verification over the temporary minimal-equivalent secS challenge contract after `local_static`; full Castalia Wallet wallet-core import/parity remains future reconciliation work. | Do not claim production wallet auth, full wallet-core integration, live Castalia Wallet parity, or trusted issuer/root policy from this verifier alone. |
| Midnight / generic ZK proof adapter | Future after public inputs and statement meaning are defined. | Do not claim current proof bytes are a meaningful ZK proof. |
| Dregg authority rail beyond the M12.3 shape seam | Future after capability, revocation, root freshness, and semantic proof/capability validation are defined. M12.3 implements only the Dregg-shaped receipt/capability-reference shape + author-signature seam. | Do not describe M12.3 shape/signature verification or M14 `dregg_backed` fixtures as M15 `dregg_authority`, Dregg consensus/finality/capability/revocation authority, or live Dregg discovery. |
| Cardano settlement evidence | Future for capital/settlement operations only. | Do not treat Cardano as generic RPC verification. |
| Public chain anchoring of receipts | Future external proof/settlement rail. | Do not claim current SQLite telemetry is public auditability. |

## Out-of-scope for this repo

secS-magik does not own:

- Gallery product policy;
- app/browser login UX;
- ordinary WalletAuth HTTP sessions;
- Dregg consensus;
- Midnight circuits themselves;
- Cardano settlement/business logic;
- auction/business logic;
- arbitrary shell access;
- centralized Hub orchestration;
- Castalia membership semantics as a product authority.

## Required language discipline

Use these phrases:

- “current prototype” for code that exists but is incomplete;
- “target verifier pipeline” for accepted architecture not yet implemented;
- “configured signed context/receipt posture through B4” for current Ed25519 contexts/receipts plus own-verifier key status checks, while still caveating that issuer/root trust policy and live registry discovery are not complete;
- “local/dev/test scaffold” for `local_static` and plaintext modes;
- “temporary minimal-equivalent secS challenge contract” for the current Track D wallet verifier until Castalia Wallet wallet-core parity is reconciled;
- “client-side / outgoing-call surface” for local Hermes/secC/secZ;
- “secS verifier/RPC substrate” for the corrected server-side boundary.

Avoid these phrases unless code proves them:

- “production-secure”;
- “fully ZK-verified”;
- “secZ verifies authority”;
- “Dregg implements this path”;
- “WalletAuth is part of secS-magik”;
- “manifest is the firewall” without caveating that current bindings are prototype hardcoded registrations;
- “production receipt ledger” or “public auditability” for the current local SQLite receipt/event ledger.
